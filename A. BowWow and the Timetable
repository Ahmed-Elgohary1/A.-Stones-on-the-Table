#include <bits/stdc++.h>
using namespace std;
 
int main() {
	
	string s;
	cin >> s;
	int n = s.size() - 1;
	if(n % 2 == 1 || count(s.begin(), s.end(), '1') > 1)
		cout << n / 2 + 1;
	else
		cout << n / 2;
	return 0;
}
